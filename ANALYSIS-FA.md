# تحلیل آموزشی و طرح اپ «دهکدهٔ ریاضی» (ریاضی اول دبستان، چاپ ۱۴۰۴)

> قانون طلایی: کتاب را به اپ تبدیل نکردیم. از کتاب یاد گرفتیم «چه مفهومی، با چه ترتیبی، با چه فعالیتی»؛ بعد فقط کلیدی‌ترین تمرین‌ها را تعاملی کردیم.

## یافته‌های کلیدی از کتاب (۲۵ بخش، ۱۸۴ صفحه)
- **نماد دیر می‌آید.** رقم ۱ تازه در ص ۴۵ معرفی می‌شود؛ قبلش ۴۴ صفحه شمارش، الگو، چوب‌خط و «مفهوم» جمع و تفریق بدون نماد است. علامت + در ص ۸۶ و − در ص ۹۳ می‌آید.
- **مسیر ثابت کتاب:** شیء ملموس (توصیهٔ پایین هر صفحه) ← تصویر ← چوب‌خط/چینه/انگشت ← عدد ← نماد ← محور ← مسئله.
- **چوب‌خط با دستهٔ ۵تایی** از ص ۲۴ تا آخر کتاب ابزار اصلی نمایش مقدار، جمع و تفریق است.
- **ساختار پنج:** ۶=۵و۱، ۷=۵و۲، ۸=۵و۳ (ص ۶۷ تا ۸۱) پایهٔ ده‌سازی.
- **«ادامه بشمار، از یک شروع نکن»** (ص ۱۶ و ۱۹) نشانهٔ آمادگی برای جمع است.
- **الگو در تقریباً هر بخش** تکرار شده و از «ادامه بده» به «بخش تکرارشونده را پیدا کن» (ص ۳۴، ۳۹) و بعد الگوی عددی (۲تا۲تا، ۵تا۵تا، ۱۰تا۱۰تا، ص ۱۲۲ به بعد) می‌رسد.
- **جدول‌های شگفت‌انگیز** (لاتین‌اسکوئر رنگی/شکلی/عددی) مدام برای استدلال سطر و ستون آمده‌اند.
- **صفر** غیرمستقیم در تفریق (ص ۳۳) و رسمی در ص ۶۲.
- **مقایسه:** اول تناظر یک‌به‌یک و «بیشتر/کمتر»، بعد مساوی کردن، بعد نماد = و بعد < >.
- **ده‌تایی و یکی** از بخش ۱۵ (ص ۱۰۰)، محور اعداد برای جمع و تفریق از ص ۱۱۹، مسئلهٔ تصویری با «شکل بکش / روی محور نشان بده» از ص ۱۲۰.
- **فعالیت‌های کم‌ارزش برای اپ:** کار با شابلون، قیچی، خط‌کش، رنگ‌آمیزی قرینه، ساعت و تقویم (مهارت حرکتی یا وابسته به ابزار واقعی؛ در اپ یا بی‌معنی‌اند یا اولویت پایین دارند). تمرین‌های تکراری «بشمار و بنویس» با عددهای مختلف در اپ به‌صورت تولید خودکار عدد جایگزین شده‌اند، نه صفحه‌به‌صفحه.

## A. نقشهٔ مهارت‌های ضروری (با پیش‌نیاز)
| # | مهارت | پیش‌نیاز |
|---|---|---|
| ۱ | شمارش یک‌به‌یک و کاردینالیتی (عدد آخر = کل) | — |
| ۲ | دیدن سریع تعداد ≤۶ (subitizing) | ۱ |
| ۳ | ساختن مجموعه با تعداد معین؛ نمایش عدد با شیء/چوب‌خط/ده‌تایی | ۱ |
| ۴ | یکی بیشتر/کمتر، قبل و بعد، ترتیب، شمارش رو به جلو و عقب | ۱، ۳ |
| ۵ | مقایسه (تناظر ← مساوی کردن ← نماد) | ۱، ۴ |
| ۶ | الگو (بعدی ← جای خالی ← اشتباه ← واحد تکرار ← الگوی عددی) | — |
| ۷ | جدول اعداد (پیدا کردن، جای خالی، حرکت ±۱ و ±۱۰، پرش) | ۴، ۶ |
| ۸ | ترکیب و تجزیه (کل و جزء، ساختن ۱۰) | ۳، ۹، ۱۰ |
| ۹ | جمع: ترکیب ← چوب‌خط ← ده‌تایی رنگی ← عدد | ۱ (ادامه‌شمردن) |
| ۱۰ | تفریق: برداشتن ← خط زدن ← عدد | ۱، ۴ |
| ۱۱ | محور: جلو/عقب رفتن | ۴، ۹، ۱۰ |
| ۱۲ | ده‌تایی و یکی (۱۱ تا ۵۹) | ۳ (چوب‌خط) |
| ۱۳ | حل مسئلهٔ تصویری (اضافه شد/کم شد ← عبارت) | ۹، ۱۰، ۱۱ |
| ۱۴ | شکل (گوشه/لبه) و اندازه (بلندتر، واحد غیراستاندارد) | ۱ |

## B. فعالیت‌های کتاب که ارزش تبدیل دارند
شمارش و «سریع بگو» (ص ۲، ۱۰)، یکی اضافه/کم (ص ۱۲، ۱۴)، ادامه‌شمردن (ص ۱۶، ۱۹)، چوب‌خط (ص ۲۴، ۲۷، ۳۰)، تفریق با انگشت/چوب‌خط/چینه (ص ۳۱، ۳۸، ۴۰)، الگو و واحد تکرار (ص ۴، ۳۴، ۳۹، ۴۶)، جدول شگفت‌انگیز (ص ۹، ۱۳، ۵۶)، عدد با گیره به‌عنوان طول (ص ۵۵)، مقایسه و مساوی کردن (ص ۷۰، ۷۲، ۷۳، ۸۰)، ساختار ۵ (ص ۶۷–۸۱)، دو عددی که با هم X می‌شوند (ص ۸۷، ۹۰)، جمع با مربع‌های رنگی (ص ۸۸)، ده‌تایی و یکی (ص ۱۰۰–۱۱۱)، محور (ص ۱۱۹)، الگوی عددی (ص ۱۲۱–۱۳۸)، دو رنگ برای ده‌سازی (ص ۱۲۶)، مسئله با شکل و محور (ص ۱۳۰، ۱۳۳)، جدول صدتایی و پرش (ص ۱۴۲–۱۵۶).

## C. تمرین‌های اپ (۵۱ الگوی تمرینی؛ هر کدام ۵ دور با عدد تازه)
جزئیات هر تمرین (نام، هدف، سطح، پیش‌نیاز، نوع تعامل) مستقیم در `src/math/exercises.ts` است و در اپ هم با دکمهٔ 👪 (راهنمای والدین) دیده می‌شود. خلاصه:

| خانه | تمرین‌ها (سطح) | چرا |
|---|---|---|
| شمارش | لمس کن و بشمار (۱)، اشیای پراکنده (۲)، سریع بگو (۲)، خرگوش درست شمرد؟ (۳)، ادامه بشمار (۳) | به‌جای «چند سیب؟» کودک لمس می‌کند، شماره روی شیء می‌آید و صدا عدد را می‌گوید؛ خطای شمارش را خودش کشف می‌کند |
| ساختن عدد | به همین تعداد بگذار (۱)، چوب‌خط بکش (۲)، کدام کارت؟ (۲)، پنج و چند؟ (۳) | از عدد به مقدار؛ یک عدد در چهار نمایش |
| قبل و بعد | یکی آمد/رفت (۱)، قطار قبل و بعد (۲)، جای خالی (۲)، شمارش معکوس (۳)، از کوچک به بزرگ (۳) | ترتیب و همسایگی عدد، پیش‌نیاز محور |
| مقایسه | کدام بیشتر؟ با جفت شدن (۱)، مساوی‌اش کن (۲)، < > = (۳) | دستهٔ کمتر گاهی پهن‌تر چیده می‌شود تا کودک به ظاهر تکیه نکند |
| ده‌تایی‌ها | ده‌تا را ببند (۲)، عدد را با بسته بساز (۳) | ده یکی خودکار یک بسته می‌شود |
| الگو | بعدی (رنگی/شکلی/تصویری/حرکتی) (۱)، جای خالی (۲)، قطعهٔ اشتباه (۳)، واحد تکرار (۳)، الگوی عددی (۳) | نظم و رابطه، نه تزئین |
| جدول عددها | پیدا کن (۱)، خانهٔ خالی (۲)، حرکت ±۱/±۱۰ (۳)، رنگ با پرش (۳) | کشف ارتباط عددها روی جدول |
| جدول شگفت‌انگیز | رنگی ۳×۳ (۲)، عددی ۴×۴ (۳) | استدلال سطر/ستون (از کتاب) |
| شکل‌ها | گوشه‌ها را لمس کن و بشمار (۱)، کدام شکل؟ (۲) | شکل از روی ویژگی، نه نام |
| اندازه | بلندتر/کوتاه‌تر (۱)، چند گیره؟ (۲) | عدد به‌عنوان طول |
| جمع | دو دسته یک سبد (۱)، با چوب‌خط (۲)، خانه‌های رنگی (۲)، با عدد + کمک تصویری (۳)، «یک جمع، چند شکل» (۳) | بعد از هر جواب، عبارت ۲ + ۳ = ۵ کنار تصویر ظاهر و خوانده می‌شود |
| تفریق | پرنده‌ها پریدند (۱)، خط زدن چوب‌خط (۲)، با عدد (۳)، «یک تفریق، چند شکل» (۳) | کودک خودش برمی‌دارد؛ صفر هم پیش می‌آید |
| محور | جلو بپر (۲)، عقب بپر (۲)، عبارت را بپر تا ۲۰ (۳) | هر پرش را خود کودک می‌زند؛ جهت اشتباه بازخورد مفهومی دارد |
| ساختن ده | زیر کاسه چند تا؟ (۲)، ده را کامل کن (۲)، یک عدد چند جمع (۳) | کل و جزء؛ پایهٔ ده‌سازی |
| مسئله | قصهٔ تصویری: اول «بیشتر شد/کمتر شد» بعد جواب (۲)، انتخاب عبارت قصه (۳) | فهم موقعیت قبل از حساب |

## D. اولویت‌بندی
- **ضروری (۳۱)** و **مهم (۲۰)**: همه در نسخهٔ ۱ پیاده شده‌اند (فیلد `priority`).
- **اختیاری (نسخه‌های بعد، پیاده نشده):** ساختن الگوی شخصی، قرینه/شابلون، ساعت و تقویم، سنگین‌تر/سبک‌تر، ترتیب کارها، جمع سه‌عددی، شمارش ۳تا۳تا/۴تا۴تا/۶ تا ۸، جمع و تفریق مضرب‌های ۱۰، مسئله با اطلاعات اضافه، نمایش انگشتی (بهتر است والد با دست واقعی تمرین کند).

## E. ساختار اپ و مسیر یادگیری
صفحهٔ شروع ← نقشهٔ ۳ جزیره (عددها / نظم و الگو / جمع و تفریق) ← هر جزیره ۵ خانه ← هر خانه ۲ تا ۵ تمرین (سطح ۱ تا ۳) ← اجرای ۵ دور ← ستاره (۱ تا ۳).
دکمهٔ **«🎯 پیشنهاد امروز»** مسیر سراسری کتاب را دنبال می‌کند (`LEARNING_PATH` در `src/math/progress.ts`): شمارش و الگو هم‌زمان شروع می‌شوند، بعد نمایش‌ها، مقایسه، جمع/تفریق مفهومی، نماد، محور و مسئله. قفل سخت نداریم؛ تمرین بدون پیش‌نیاز فقط کم‌رنگ و با پیام «بهتر است اول ... را بازی کنی» نمایش داده می‌شود. «یاد گرفته» = حداقل ۲ ستاره.

## F. معماری فنی
**حفظ شد (بدون تغییر یا با تغییر جزئی):** صفحهٔ شروع و منوی دفترچه، تنظیمات صدا/موسیقی، پنجرهٔ خروج، سیستم برگشت گوشی (`backNav`)، هدر بازی، دکمه‌های کارتونی تأیید/بستن، حباب بازخورد، نقشه‌های عمودی و خانه‌ها و جزیره‌ها (همان نقاشی و جای‌ها)، `ScoreIsland`، صداها و TTS، پیکربندی Capacitor و workflow ساخت APK، همهٔ CSSهای عمومی.
**حذف شد:** همهٔ منطق الفبا: `persianAlphabet`، `curriculum` فارسی، `wordBank`، `sentenceBank`، `boardWords`، `words`، `pieces`، `syllables`، `persianEngine`، `lessonState` و کامپوننت‌های `RecognitionVillage`، `WordVillage`، `SentenceBuilder`، `SyllableGame`، `MagneticBoard`، `TracePractice`، `WordGames`، `MathGames` قدیمی، `HomeScreen`، `HeaderNav`، `ProgressGarden`، `SubjectSelect`، `LessonPicker`.
**اضافه شد:**
- `src/math/types.ts` مدل داده (id, skill, level, type, question, objects, options, answer, feedback, difficulty, prerequisite, priority, params, fixed)
- `src/math/exercises.ts` کاتالوگ تمرین‌ها (فقط داده)
- `src/math/generators.ts` سازندهٔ دور برای هر type، با سخت‌شدن تدریجی داخل هر تمرین
- `src/math/curriculum.ts` جزیره‌ها و خانه‌ها · `src/math/progress.ts` ستاره‌ها، پیش‌نیاز، مسیر پیشنهادی
- `src/components/math/ExerciseRunner.tsx` موتور اجرا (دورها، بازخورد، ستاره، ذخیره)
- `src/components/math/Visuals.tsx` اجزای تصویری قابل استفادهٔ مجدد: خانهٔ ده‌تایی، چوب‌خط ۵تایی، تاس، محور اعداد، شکل، عبارت
- `src/components/math/renderers/*` نمایشگر هر نوع · `src/math-ui.css`

**افزودن تمرین تازه:** اگر نوعش موجود است، فقط یک ردیف به `exercises.ts` و یک شناسه به `LEARNING_PATH`. اگر نوع تازه است: یک generator + یک کامپوننت + یک خط در `renderers/index.ts`.

**نکته‌هایی که باید شما تأیید کنید:** جهت جدول عددها راست‌به‌چپ است (ثابت `CHART_DIR`)، عبارت‌ها و محور چپ‌به‌راست مثل کتاب. آیکن و اسپلش تازه ساخته شد ولی بهتر است نسخهٔ نهایی را طراح شما بکشد. CSSهای مخصوص الفبا (کلاس‌های syl-، trace-، wv-) هنوز در فایل‌های CSS هستند و بی‌اثرند؛ پاک‌سازی‌شان اختیاری است. به‌خاطر نبودن اینترنت در محیط من، `tsc` و `vite build` اجرا نشد؛ کد با esbuild باندل و همهٔ ۵۱ تمرین در مرورگر بدون خطا باز شدند و چند مسیر کامل تست شد. یک بار `npm install && npm run lint && npm run build` بزنید.
